import: start.yaml
---
-
  git_repos:
    command: backup
    source: /repositories
    backup_dir: {{BACKUPDIR}}/dev_servers
    backup_name: git_repos
-
  data_pypi:
    command: backup
    source: /data
    backup_dir: {{BACKUPDIR}}/dev_servers
    backup_name: pypicloud
